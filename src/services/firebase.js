/**
 * OVERLOAD Firebase Service
 * Centralized Firebase entrypoint (Phase 1 placeholder for Phase 12-14 integration)
 */

export const firebaseConfig = {
  // To be populated in Phase 12 with Firebase project configurations
  projectId: 'brain-gym',
};

export const isFirebaseInitialized = () => false;
